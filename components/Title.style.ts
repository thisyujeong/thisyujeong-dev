import styled from '@emotion/styled';

export const TitleContainer = styled.div`
  margin-bottom: 32px;

  h1 {
    margin-bottom: 12px;
    font-size: 38px;
    font-weight: 500;
    text-transform: capitalize;
  }

  p {
    font-size: 16px;
    opacity: 0.5;
  }
`;
