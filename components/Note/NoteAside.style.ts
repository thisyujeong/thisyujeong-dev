import styled from '@emotion/styled';

export const NoteAsideContainer = styled.section`
  position: absolute;
  top: -12px;
  left: -240px;
  width: 240px;
  padding: 8px 0;
  padding-right: 24px;
`;
